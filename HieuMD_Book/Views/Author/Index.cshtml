@model IEnumerable<BookStoreManagementCodeFirst.Author>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-bordered">
    <tr class="success">
        <th>
            @Html.DisplayNameFor(model => model.AuthorName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.History)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AuthorName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.History)
        </td>
        <td style="width:100px;height:20px">
            @Html.ActionLink(" ", "Edit", new { id = item.AuthorId }, new { @class = "fas fa-edit" }) |
      
          <a href="@Url.Action("Delete", "Author", new { @id = item.AuthorId })"  class="fas fa-trash-alt" onclick="return confirm('Are you sure')"></a>

           
        </td>
    </tr>
}

</table>
